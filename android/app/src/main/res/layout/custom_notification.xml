<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="#FFFFFF">

    <!-- Header Section (Left) -->
    <LinearLayout
        android:id="@+id/header_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/notification_right_icon">

        <TextView
            android:id="@+id/notification_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Peak time starts for you"
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/notification_body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Open the app now to get results."
            android:textColor="#333333"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Thumbnail (Right) -->
    <ImageView
        android:id="@+id/notification_right_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:src="@mipmap/ic_launcher_round"
        android:scaleType="centerCrop" />

    <!-- HUGE SCHMOOZE-STYLE TIMER (Center) -->
    <Chronometer
        android:id="@+id/notification_timer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/header_text"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:countDown="true"
        android:textColor="#6082B6" 
        android:textSize="64sp"
        android:textStyle="bold" />

    <!-- Action Link (Bottom Left) -->
    <TextView
        android:id="@+id/notification_action_link"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/notification_timer"
        android:layout_marginTop="8dp"
        android:text="Join the fun"
        android:textColor="#1A73E8"
        android:textSize="14sp"
        android:textStyle="bold" />

</RelativeLayout>
